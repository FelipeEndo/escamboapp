$(function(){
  let $sendMailModal = $('#sendMailModal');
  
  $('#subject-text').val('');
  $('#message-text').val('');
  $sendMailModal.modal('hide');
  $.notify('<%= @notify_message %>', '<%= @notify_flag %>');
});
